/**
 * Lightweight footer matching the header rhythm.
 */
export const SiteFooter = () => (
  <footer className="mt-auto border-t border-black/10 py-8 text-center text-xs text-zinc-500 dark:border-white/15 dark:text-zinc-400">
    Built with Next.js. Configure auth, database, and uploads in `.env.local`.
  </footer>
);
